pipeline {
    agent any
    stages {
        stage("checkout")
        {
            steps 
            {
                // Get some code from a GitHub repository
  git (url: 'https://github.com/SnehaGhongade/JobPortalfrontend.git') {

            }
        
        }
        stage("Build") {
            steps {
                git (url: 'https://github.com/SnehaGhongade/JobPortalfrontend.git') {
                    script {
                        // Build the .NET project
                        sh 'dotnet publish -c Release -o out'
                    }
                }
            }
        }
        stage('Test') {
            steps {
                sh 'make test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'make publish'
            }
        }
    }
}}