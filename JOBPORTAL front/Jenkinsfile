pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                git (branch: 'main', url: 'https://github.com/SnehaGhongade/JobPortalfrontend.git') {
                    script {
                        // Build the .NET project
                        sh 'dotnet publish -c Release -o out'
                    }
                }
            }
        }
        stage('Test') {
            steps {
                //
            }
        }
        stage('Deploy') {
            steps {
                //
            }
        }
    }
}